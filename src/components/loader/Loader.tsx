import { LoadingSpinnerContainer, LoadingSpinner } from "./LoaderStyles";

const Loader = () => {
  return (
    <LoadingSpinnerContainer>
      <LoadingSpinner></LoadingSpinner>
    </LoadingSpinnerContainer>
  );
};

export default Loader;
